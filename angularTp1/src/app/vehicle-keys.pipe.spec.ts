import { VehicleKeysPipe } from './vehicle-keys.pipe';

describe('VehicleKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
